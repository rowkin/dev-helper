# DevHelper 更新日志

## v1.4.0 (2026-07-07)

### 🚀 新功能

* **新增「IP 与网速检测」工具 (⌥6)**：
  * **IP 地址智能探测**：支持基于 WebRTC 的本地局域网 IP (Local IP) 智能穿透探测，以及通过安全公网 API 动态识别外网 IP (Public IP)、ISP 运营商及所属国家/省份/城市等地理位置。
  * **高精度网络基准跑分**：具备毫秒级 Ping（延迟）与 Jitter（抖动）测试，并通过 CDN 及安全上传端点动态计算网页实时下载速率 (Download Speed) 与上传速率 (Upload Speed)。
  * **AI 智能网络状态体检**：无缝对接 AI 诊断面板，自动解析网速及延迟数据，由大模型给出智能优化、DNS调整及网络异常诊断意见。
  * **网速历史数据持久化**：使用 `chrome.storage.local` 本地存储机制，自动保存最近 30 条网速检测历史记录，方便跟踪网络质量波动趋势。

### ⚙️ 优化与修复

* **截图工具功能升级**：
  * 重构并优化全页面（长图滚动）截图的滚动捕获算法，解决部分动态滚动条或粘性定位元素干扰导致的长图遗漏问题。
  * 升级 AI 截图分析框组件样式，使用优雅的高度动态计算与局部滚动机制，消除在左侧画布过高时右侧大量空白的问题；全面支持 AI 诊断分析结果以 Markdown 格式高保真渲染展示。
* **常驻 AI 助手交互打磨**：
  * 修复常驻 AI 聊天输入框由于内容过多引起视口溢出的问题，自适应折行增加高度。
  * 新增机器人回复时的流式 Loading 骨架屏态与流畅打字机动效，提升人机协作沉浸感。
* **版本与数据全局对齐**：
  * 全局同步升级 `manifest.json`、`package.json`、Popup 主页、设置项及 site 落地页的版本号至 `v1.4.0`，工具计数同步升级至 `19`。

## v1.3.2 (2026-07-02)

### 🚀 新功能

* **新增「屏幕截图」工具**：
  * **三种捕获模式**：一键捕获网页可视区域，自动滚动并智能拼接生成网页整页长图，或者自由框选网页指定区域。
  * **按需冷启动设计**：截图辅助脚本仅在用户触发截图时动态注入网页，使用完毕后即刻卸载销毁，彻底消除不必要的常驻内存占用。
  * **全能 Canvas 标注编辑器**：内置画笔、矩形、箭头、文字、马赛克及撤销 (Undo/Clear) 涂鸦工具，一键下载或复制标注后的图片，操作流畅极速。
  * **AI OCR 文本提取与大模型协作**：将截图完美关联至右侧双栏 AI 协同助手，支持一键发送给本地/云端大模型进行 UI 样式还原、代码审查与文字提取。

### ⚙️ 优化与修复

* **版本全局同步**：
  * 将 `manifest.json`、`package.json`、Popup 页脚、设置面板及 site 落地页的版本号完美同步升级至 `v1.3.2`，工具总数同步更新至 `18`。

## v1.3.1 (2026-06-30)

### ⚙️ 优化与修复

* 消除多次重复载入脚本导致的 SyntaxError 错误：
  * 彻底清理全部工具 HTML 页面中由于重复引入 `ai-service.js` 和 `common.js` 导致的 `SyntaxError: Identifier 'AI_PROVIDERS' has already been declared` 及 `LOGO_SVG` 重复声明报错。
* Chrome 128+ 内置 AI API 规范适配：
  * 在 `js/ai-service.js` 和 `js/tools/index.js` 的 `create` 调用中加入最新的 `expectedOutputs: [{ type: 'text', languages: ['en'] }]` 参数声明，彻底消除 Chrome 控制台上报的 `No output language was specified in a LanguageModel API request` 警告。
* 新增磁盘可用空间不足的温馨提示：
  * 在唤起本地 AI 模型下载的提示、Toast 及配置 Flags 指引中，增加模型下载需要 10GB 以上磁盘可用空间的温馨说明，帮助用户在遇到磁盘空间不足导致模型下载失败时快速定位。

## v1.3.0 (2026-06-29)

### 🚀 新功能

* **颜色转换工具深度扩展**：
  * **屏幕级吸色 (EyeDropper)**：正式支持 Chrome 原生 `EyeDropper` API。点击“滴管拾色”后可触发高精度放大镜，支持吸取当前网页、其他浏览器标签页、甚至浏览器窗口外的桌面任意像素颜色。

### ⚙️ 优化与修复

* **AI 表格流式渲染增强**：
  * 重构 `renderInlineMarkdown` 函数，增加占位符提取和流式表格逐行扫描识别机制，完美解决 AI 面板生成 Markdown 表格无法渲染、换行导致布局塌陷的问题。
* **Gemini Nano 报错修复**：
  * 针对 LanguageModel 实例创建时 expectedOutputLanguages 对 'zh' 的类型抛错问题进行修复，对齐 Chrome 最新标准，消除控制台警告。
* **版本同步**：
  * 全局同步更新 `manifest.json`、`package.json`、Popup 及全景控制台的版本信息为 `v1.3.0`。

## v1.2.0 (2026-06-29)

### 🚀 新功能

* **新增「页面性能检测」工具 (⌥5)**：
  * **核心指标监控**：通过 `PerformanceObserver` 及 `performance.timing` 接口，深度监控目标页面的首次绘制 (FP)、首次内容绘制 (FCP)、DOMContentLoaded (DOM就绪) 及 Load Time (完全加载) 指标。
  * **网络时序瀑布图**：使用高对比度的 HSL 渐变色瀑布时序条，精准还原重定向、DNS查询、TCP握手、SSL握手、TTFB (首字节响应)、内容下载等各个阶段的相对时序耗时。
  * **智能性能体检评分**：基于 Web Vitals 规范阈值，以精美的环状进度图为核心指标实时评分。
  * **定制化调优方案**：内置算法针对 DNS 延迟、握手时间、服务器相应 (TTFB)、DOM 阻塞及内存泄漏提供定制化调优方案清单。
  * **AI 智能专家诊断**：无缝对接 DevHelperAI 助手。在性能页面中一键唤醒 AI，自动传输时序 JSON 数据包，由大模型为您深度剖析网页加载瓶颈，给出代码及服务器配置级别的具体优化方案。

### ⚙️ 优化与修复

* **导航系统升级**：
  * 在 Popup 快捷菜单与全景控制面板 (`index.html`) 中正式注册该功能，并将内置工具总数更新至 17 个。
  * 优化 `popup.js`，用户在被测页面中打开扩展时，会自动记录并携带当前活动页的 Tab ID 参数，实现“一键快速分析”。
* **工程规范对齐**：
  * 全局同步更新 `manifest.json`、`package.json`、Popup 版权栏及设置项中的版本号为 `v1.2.0`。
  * 所有新增页面及事件逻辑均符合 Chrome Extension MV3 及 CSP 安全规范，禁止使用任何内联脚本。

## v1.1.0 (2026-06-22)

* 首个正式版本发布，集成 JSON 格式化、编解码、API 调试、二维码、时间戳等 16 项常用开发者工具。
