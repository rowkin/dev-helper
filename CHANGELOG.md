# DevHelper 更新日志

## v1.5.1 (2026-07-17)

### ⚙️ v1.5.1 优化与修复

* **一键 AI 智能诊断交互与渲染适配升级**：
  * **友好加载动效**：在 BMI 诊断等待时引入带有 CSS 旋转效果的 Loading 动画，并在请求发送期间禁用诊断按钮（显示“⚡ 诊断中...”），提供防重触交互。
  * **富文本渲染支持**：利用内置的 `renderInlineMarkdown` 引擎对 BMI 流式返回数据进行富文本渲染，确保膳食、运动计划中的加粗与列表样式严格排版。
* **AI 引擎调用兼容性修复**：
  * **内置 AI 服务修复**：在 `js/ai-service.js` 中补全了遗失的 `callBuiltinAI` 函数实现，确保切换到 Chrome 内置 AI (Gemini Nano) 时能够流畅提问对话而不再报错。
  * **推理模型参数自适应**：针对以 `kimi-k`、`o1-` 等命名的严格限制 temperature 参数的推理大模型，自动在请求体中剔除 `temperature` 参数，彻底解决了 Kimi/OpenAI 推理模型下报 `invalid temperature`（400 Bad Request）的异常。
* **版本与站点全线升级**：
  * 将 `manifest.json`、`package.json`、Popup 弹出页、主页关于面板及落地页站点 `site/index.html` 一体化升级至 `v1.5.1`。

## v1.5.0 (2026-07-17)

### 🚀 v1.5.0 新功能

* **万能计算器 (Universal Calculator) 深度升级**：
  * **标准与科学计算器**：基于无 `eval` 的 Shunting-yard 算法与 RPN 栈求值引擎，完全符合 Chrome MV3 极其严格的 Content Security Policy (CSP) 安全规范；支持隐式乘法自动补全（如 `3(4+5)` 自动解析为 `3*(4+5)`）；提供三角函数（角度制）、对数（ln, log）以及平方根等科学运算。
  * **实时汇率换算**：接入免费开放的 Exchange Rates API。内置了完备的降级与离线备份方案——在网络异常或接口受限时，自动加载 `chrome.storage.local` 历史缓存汇率，若缓存亦不可用则降级为出厂硬编码备份汇率，保证转换服务的 100% 连续性；提供以 100 CNY 为基准的常见币种双向对比表格与双向金额折算。
  * **房贷月供推演**：支持“等额本息”与“等额本金”两种经典还款路径的推演；支持自定义还款期数；自动计算首月月供、每月递减、总还款利息及总还款额，并生成逐期本息递减、待还剩余款项的详细还款明细推演表。
  * **物理单位换算**：新增长度、重量、面积、体积、温度、速度六大维度的物理单位一键对齐功能，采用本地高精度浮点数转换，输入任意数值，其他所有单位实时同步响应，性能极佳。
  * **财务大写与 BMI 计算**：集成人民币财务大写快速转换器（遵循银行防伪防篡改的汉字数字规范，自动补全元角分整），以及 BMI 身体质量指数计算器（支持身高体重输入与多维健康评估意见）。
  * **AI 财务与数学智能诊断**：与 AI 智能解读抽屉深度绑定。支持对输入的数学公式含义进行讲解，科普币种宏观经济与跨境消费贴士，并能基于房贷利息差额、物理单位背景、健康 BMI 管理及大写金额规范为用户提供高度定制化的 AI 智能分析。

### ⚙️ v1.5.0 优化与修复

* **AI 助手本地 Mock 仿真升级**：
  * 针对本地双击预览调试等缺失 `chrome` 原生 API 的脱机环境，重构了 `js/ai-service.js` 的 `fetch` 拦截 Mock 数据，增加了对“计算器/汇率/房贷/单位换算/BMI”多面板上下文的感应拦截。现在在本地非插件状态预览时，AI 助手也能返回针对当前计算面板的高质量模拟诊断报告，消存在原有通用闲聊词的突兀感。
* **版本与工具全局升级**：
  * 修复了 `site/index.html` 落地页上页脚静态版本显示滞后在 `v1.4.1` 的历史问题，实现全站彻底对齐 `v1.5.0`。
  * 将 `site/index.html` 落地页工具展示库中加入了万能计算器的元数据，保证公开资产一致性。
  * 升级 `manifest.json`、`package.json`、Popup 主页及关于面板的版本号至 `v1.5.0`，工具计数由 20 升级至 21，完成新一轮发布资产的封装。

## v1.4.1 (2026-07-08)

### 🚀 v1.4.1 新功能

* **图片放大查看器 (Lightbox)**：
  * **高拟真手势交互**：支持对放大后的图片进行鼠标拖拽平移及鼠标滚轮无限缩放，便于定位查看长截图中的微小细节。
  * **沉浸式控制面板**：基于毛玻璃特效 (Glassmorphism Modal)，在顶部悬浮微型控制栏，支持放大、缩小、1:1 复位、顺时针 90 度旋转、以及一键下载/保存大图文件。
* **常驻 AI 助手删除能力升级**：
  * **历史对话单项删除**：左侧历史对话列表增加了悬浮触发的删除按钮，删除当前对话后会自动向后加载或重新开启一个干净的会话。
  * **单个气泡精准删除**：每个聊天气泡右上角均新增了微型悬浮删除小按钮，点击会从本地存储中彻底剔除并更新整个聊天流的索引序列。

### ⚙️ v1.4.1 优化与修复

* **大资产缓存持久化优化 (解决截图刷新丢失)**：
  * 引入了基于 `mediaAssets` 的对话大图 Base64 字典机制。当生成二维码或长截图时，在本地自动转换为带时间戳后缀的唯一 ID 并存储在对应会话中。
  * 刷新页面加载历史时，大图可以完美持久化呈现，且在同一会话中多次截图/生成二维码之间独立渲染，互不覆盖。
* **工具集 Manifest 深度对齐**：
  * 重新整理并全量补充了 DevHelper 插件内实际上已完整搭载的全部 20 大内置开发者工具（如 API 接口测试、代码美化、正则测试等）。
  * 将 20 大工具完整注入 System Prompt 的静态上下文，并且同步更新了 Mock AI 对话大模型的功能列表展示与介绍逻辑。
* **版本全局对齐**：
  * 全局同步升级 `manifest.json`、`package.json`、Popup 页脚、设置面板及 site 落地页的版本号至 `v1.4.1`，工具总数同步更新至 `20`。

## v1.4.0 (2026-07-07)

### 🚀 v1.4.0 新功能

* **新增「IP 与网速检测」工具 (⌥6)**：
  * **IP 地址智能探测**：支持基于 WebRTC 的本地局域网 IP (Local IP) 智能穿透探测，以及通过安全公网 API 动态识别外网 IP (Public IP)、ISP 运营商及所属国家/省份/城市等地理位置。
  * **高精度网络基准跑分**：具备毫秒级 Ping（延迟）与 Jitter（抖动）测试，并通过 CDN 及安全上传端点动态计算网页实时下载速率 (Download Speed) 与上传速率 (Upload Speed)。
  * **AI 智能网络状态体检**：无缝对接 AI 诊断面板，自动解析网速及延迟数据，由大模型给出智能优化、DNS调整及网络异常诊断意见。
  * **网速历史数据持久化**：使用 `chrome.storage.local` 本地存储机制，自动保存最近 30 条网速检测历史记录，方便跟踪网络质量波动趋势。

### ⚙️ v1.4.0 优化与修复

* **截图工具功能升级**：
  * 重构并优化全页面（长图滚动）截图的滚动捕获算法，解决部分动态滚动条或粘性定位元素干扰导致的长图遗漏问题。
  * 升级 AI 截图分析框组件样式，使用优雅的高度动态计算与局部滚动机制，消除在左侧画布过高时右侧大量空白的问题；全面支持 AI 诊断分析结果以 Markdown 格式高保真渲染展示。
* **常驻 AI 助手交互打磨**：
  * 修复常驻 AI 聊天输入框由于内容过多引起视口溢出的问题，自适应折行增加高度。
  * 新增机器人回复时的流式 Loading 骨架屏态与流畅打字机动效，提升人机协作沉浸感。
* **版本与数据全局对齐**：
  * 全局同步升级 `manifest.json`、`package.json`、Popup 主页、设置项及 site 落地页的版本号至 `v1.4.0`，工具计数同步升级至 `19`。

## v1.3.2 (2026-07-02)

### 🚀 v1.3.2 新功能

* **新增「屏幕截图」工具**：
  * **三种捕获模式**：一键捕获网页可视区域，自动滚动并智能拼接生成网页整页长图，或者自由框选网页指定区域。
  * **按需冷启动设计**：截图辅助脚本仅在用户触发截图时动态注入网页，使用完毕后即刻卸载销毁，彻底消除不必要的常驻内存占用。
  * **全能 Canvas 标注编辑器**：内置画笔、矩形、箭头、文字、马赛克及撤销 (Undo/Clear) 涂鸦工具，一键下载或复制标注后的图片，操作流畅极速。
  * **AI OCR 文本提取与大模型协作**：将截图完美关联至右侧双栏 AI 协同助手，支持一键发送给本地/云端大模型进行 UI 样式还原、代码审查与文字提取。

### ⚙️ v1.3.2 优化与修复

* **版本全局同步**：
  * 将 `manifest.json`、`package.json`、Popup 页脚、设置面板及 site 落地页的版本号完美同步升级至 `v1.3.2`，工具总数同步更新至 `18`。

## v1.3.1 (2026-06-30)

### ⚙️ v1.3.1 优化与修复

* 消除多次重复载入脚本导致的 SyntaxError 错误：
  * 彻底清理全部工具 HTML 页面中由于重复引入 `ai-service.js` 和 `common.js` 导致的 `SyntaxError: Identifier 'AI_PROVIDERS' has already been declared` 及 `LOGO_SVG` 重复声明报错。
* Chrome 128+ 内置 AI API 规范适配：
  * 在 `js/ai-service.js` 和 `js/tools/index.js` 的 `create` 调用中加入最新的 `expectedOutputs: [{ type: 'text', languages: ['en'] }]` 参数声明，彻底消除 Chrome 控制台上报的 `No output language was specified in a LanguageModel API request` 警告。
* 新增磁盘可用空间不足的温馨提示：
  * 在唤起本地 AI 模型下载的提示、Toast 及配置 Flags 指引中，增加模型下载需要 10GB 以上磁盘可用空间的温馨说明，帮助用户在遇到磁盘空间不足导致模型下载失败时快速定位。

## v1.3.0 (2026-06-29)

### 🚀 v1.3.0 新功能

* **颜色转换工具深度扩展**：
  * **屏幕级吸色 (EyeDropper)**：正式支持 Chrome 原生 `EyeDropper` API。点击“滴管拾色”后可触发高精度放大镜，支持吸取当前网页、其他浏览器标签页、甚至浏览器窗口外的桌面任意像素颜色。

### ⚙️ v1.3.0 优化与修复

* **AI 表格流式渲染增强**：
  * 重构 `renderInlineMarkdown` 函数，增加占位符提取和流式表格逐行扫描识别机制，完美解决 AI 面板生成 Markdown 表格无法渲染、换行导致布局塌陷的问题。
* **Gemini Nano 报错修复**：
  * 针对 LanguageModel 实例创建时 expectedOutputLanguages 对 'zh' 的类型抛错问题进行修复，对齐 Chrome 最新标准，消除控制台警告。
* **版本同步**：
  * 全局同步更新 `manifest.json`、`package.json`、Popup 及全景控制台的版本信息为 `v1.3.0`。

## v1.2.0 (2026-06-29)

### 🚀 v1.2.0 新功能

* **新增「页面性能检测」工具 (⌥5)**：
  * **核心指标监控**：通过 `PerformanceObserver` 及 `performance.timing` 接口，深度监控目标页面的首次绘制 (FP)、首次内容绘制 (FCP)、DOMContentLoaded (DOM就绪) 及 Load Time (完全加载) 指标。
  * **网络时序瀑布图**：使用高对比度的 HSL 渐变色瀑布时序条，精准还原重定向、DNS查询、TCP握手、SSL握手、TTFB (首字节响应)、内容下载等各个阶段的相对时序耗时。
  * **智能性能体检评分**：基于 Web Vitals 规范阈值，以精美的环状进度图为核心指标实时评分。
  * **定制化调优方案**：内置算法针对 DNS 延迟、握手时间、服务器相应 (TTFB)、DOM 阻塞及内存泄漏提供定制化调优方案清单。
  * **AI 智能专家诊断**：无缝对接 DevHelperAI 助手。在性能页面中一键唤醒 AI，自动传输时序 JSON 数据包，由大模型为您深度剖析网页加载瓶颈，给出代码及服务器配置级别的具体优化方案。

### ⚙️ v1.2.0 优化与修复

* **导航系统升级**：
  * 在 Popup 快捷菜单与全景控制面板 (`index.html`) 中正式注册该功能，并将内置工具总数更新至 17 个。
  * 优化 `popup.js`，用户在被测页面中打开扩展时，会自动记录并携带当前活动页的 Tab ID 参数，实现“一键快速分析”。
* **工程规范对齐**：
  * 全局同步更新 `manifest.json`、`package.json`、Popup 版权栏及设置项中的版本号为 `v1.2.0`。
  * 所有新增页面及事件逻辑均符合 Chrome Extension MV3 及 CSP 安全规范，禁止使用任何内联脚本。

## v1.1.0 (2026-06-22)

* 首个正式版本发布，集成 JSON 格式化、编解码、API 调试、二维码、时间戳等 16 项常用开发者工具。
